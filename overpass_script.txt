[out:xml][timeout:25];
// gather results
(
  area[name="Pune District"];
  // query for "place" "without name:mr" tags
  node["place"](area)["name:mr"!~"."](area);
);
// print results
out meta;
>;
out meta qt;
